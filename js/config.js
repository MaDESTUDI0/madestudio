// Points the static site (GitHub Pages has no backend of its own) at the
// deployed API. Change this one value when the API moves (new host,
// custom domain, paid Render instance, etc.) instead of editing every page.
window.MADE_API_BASE = 'https://made-studio-api.onrender.com';
